<!DOCTYPE html>
<html>
<head>
  <title>Betsy</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <main class="container">

    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">Betsy</a>
        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">


            <% if current_user %>
            <button type="button" class="btn btn-default navbar-btn"><%= link_to "Log Out", logout_path, method: :delete  %></button>
            <li class="active"><%= link_to "My account", user_path(current_user) if current_user.valid? %></li>
            <% else %>
            <li class="active"><%= link_to "Sign Up", new_user_path%></li>
            <button type="button" class="btn btn-default navbar-btn"><%= link_to "Log In", login_path %></button>
            <% end %>

          </ul>
          </div>
        </div>
      </nav>

      <div class="row">
        <div class="col-lg-6">
      <div class="input-group">
      <input type="text" class="form-control" placeholder="Search for...">
      <span class="input-group-btn">
      <button class="btn btn-default" type="button">Go!</button>
      </span>
      </div><!-- /input-group -->
      </div><!-- /.col-lg-6 -->
      </div><!-- /.row -->

      <%= form_tag(search_product_path, method: "get") do %>
        <%= label_tag(:q, "Search for a product") %>
      <%= text_field_tag :q %>
        <%= submit_tag("Search") %>
      <% end %>

<%=params%>

      <% if flash[:error] -%>
        <div class="alert alert-danger" role="alert">
          <p class='error'><%=h flash[:error] %></p>
        </div>
      <% end %>
      <% if flash[:notice] -%>
        <div class="alert alert-warning" role="alert">
          <p class='notice'><%=h flash[:notice] %></p>
        </div>
      <% end %>

<%= yield %>

</body>
</html>
