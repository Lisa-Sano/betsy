<h1>Hello <%= current_user.name %> all u need to know about yourself</h1>
<p>
  <%= current_user.address %>
  <%= current_user.city %>
  <%= current_user.state %>
  <%= current_user.zip %>

</p>

<%= button_to "Order Fullfilment", user_fulfillment_index_path(@user), class: "btn btn-primary", method: :get%>
<%= button_to "Edit my products", user_products_path(@user), class: "btn btn-default", method: :get%>
